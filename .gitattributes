# Git LFS rules (Unity binary assets)
# Appended by @ennamjsc/agents-scaffold game-unity profile.
# Run `git lfs install` once after this file is added.

# 3D models
*.fbx  filter=lfs diff=lfs merge=lfs -text
*.glb  filter=lfs diff=lfs merge=lfs -text
*.gltf filter=lfs diff=lfs merge=lfs -text
*.obj  filter=lfs diff=lfs merge=lfs -text

# Textures
# Note: Web image formats (.png, .jpg, .jpeg) are stored directly in Git for Vercel deployment compatibility.
*.psd  filter=lfs diff=lfs merge=lfs -text
*.tga  filter=lfs diff=lfs merge=lfs -text
*.tif  filter=lfs diff=lfs merge=lfs -text
*.tiff filter=lfs diff=lfs merge=lfs -text

# Audio
*.wav filter=lfs diff=lfs merge=lfs -text
*.ogg filter=lfs diff=lfs merge=lfs -text
*.mp3 filter=lfs diff=lfs merge=lfs -text

# Video
*.mp4 filter=lfs diff=lfs merge=lfs -text
*.mov filter=lfs diff=lfs merge=lfs -text

# Unity Smart Merge — these are TEXT (YAML), DO NOT LFS them.
# (Documented here so a future contributor does not "fix" the absence of these patterns.)
# *.unity  — scene (YAML, Smart Merge)
# *.prefab — prefab (YAML, Smart Merge)
# *.asset  — ScriptableObject (YAML, Smart Merge)
# *.meta   — import meta (YAML, Smart Merge)
